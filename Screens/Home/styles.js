import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: "center",
  },
  newsContainer: {
    flex: 1,
    alignItems: "center",
    flexBasis: 28,
  },
  categoriesContainer: {
    alignItems: "center",
    flex: 1,
  },
});
