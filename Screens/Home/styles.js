import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: "center",
  },
  listcontainer: {
    flexBasis: 340,
    flexShrink: 1,
    // marginVertical: 20,
  },
});
