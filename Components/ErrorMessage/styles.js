import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  errorTextContainer: {
    borderRadius: 10,
    padding: 10,
  },
  errorText: {
    textAlign: 'center',
    fontFamily: 'Acme',
  },
});
