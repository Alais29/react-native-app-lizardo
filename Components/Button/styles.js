import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  btn: {
    borderRadius: 50,
    paddingHorizontal: 15,
    paddingVertical: 10,
    alignItems: "center",
  },
});
